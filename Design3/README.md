# Design 3 — Creative Premium JSX React App

**Theme:** Cream + deep teal + plum, bento-grid layout, floating glass nav.

**Stack:** React + Vite + JSX + React Router

```bash
cd Design3
npm install
npm run dev
```

**Hero image:** `hero-home.png`  
**Images:** `public/Images/`
